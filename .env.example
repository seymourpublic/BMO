# Anthropic API Configuration (for Backend Server)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Fish Audio API Configuration (for Backend Server)
# Get your API key from: https://fish.audio/app/api-keys/
# This gives BMO the ACTUAL voice from Adventure Time!
FISH_AUDIO_API_KEY=your_fish_audio_key_here

# Instructions:
# 1. Copy this file to .env (without the .example)
# 2. Replace 'your_api_key_here' with your actual Anthropic API key
# 3. Replace 'your_fish_audio_key_here' with your Fish Audio API key
# 4. Never commit .env to git (it's in .gitignore)
#
# Note: Both API keys are used by the backend server (no VITE_ prefix needed)
# The frontend calls the backend, which then calls the APIs securely.
